<%= form_for(@form) do |f| %>
  <% if @form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@form.errors.count, "error") %> prohibited this form from being saved:</h2>

      <ul>
      <% @form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <!--<%= f.select :category_ids, options_from_collection_for_select(Category.all, :id, :title), {include_blank: false}, multiple: true %>-->
    <!--<%= f.collection_select :category_ids, Category.all, :id, :title, {include_blank: false}, multiple: true %>-->
    <%= f.collection_check_boxes :category_ids, Category.all, :id, :title do |b| %>
      <%= b.check_box %> <%= b.label %> <br/>
    <% end %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
